import { HeroContent } from "./sub/hero-content";

const Hero = () => {
  return (
    <div id="#about" className="relative flex flex-col h-full w-full">
      <HeroContent />
    </div>
  );
};

export default Hero;
